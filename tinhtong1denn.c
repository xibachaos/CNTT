#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(){
	int i, s=0, n;
	printf("Nhap vao so: ");
	scanf("%d", &n);

	for (i=0; i<=n; i++)
		
		if (s=s+i) 
		continue;
		printf("Tong la: %d", s);
	
	return 0;	
}
